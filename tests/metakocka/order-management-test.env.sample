# Sample environment file for Metakocka order management integration test

# Required: Authentication token for API requests
AUTH_TOKEN=your_auth_token_here

# Optional: ID of an existing order to test with
# If not provided, a test order will be created
# ORDER_ID=existing_order_id

# Optional: ID of an existing Metakocka order to test sync from Metakocka
# METAKOCKA_ID=existing_metakocka_order_id

# Optional: Base URL for API requests (default: http://localhost:3000/api)
# API_BASE_URL=http://localhost:3000/api

# Optional: Set to 'true' to enable debug logging
# DEBUG=true
