# Agent Memory Integration Test Environment Variables

# API Base URL (default: http://localhost:3000/api)
API_BASE_URL=http://localhost:3000/api

# Authentication token (required)
# This should be a valid JWT token for an authenticated user with appropriate permissions
AUTH_TOKEN=your_auth_token_here

# Agent ID (optional, will generate a random UUID if not provided)
# Use an existing agent ID if you want to test with a specific agent
AGENT_ID=

# Contact ID (optional, will generate a random UUID if not provided)
# Use an existing contact ID if you want to test with a specific contact
CONTACT_ID=
