# Sample environment variables for Stripe subscription flow tests

# API URL (default: http://localhost:3000)
API_URL=http://localhost:3000

# Authentication token (required)
# Get this from your browser's local storage after logging in
AUTH_TOKEN=your_auth_token_here

# Organization ID to test with (required)
ORGANIZATION_ID=your_organization_id_here

# Stripe Price ID to use for checkout tests
# This should match one of the price IDs configured in your SubscriptionPlans component
STRIPE_PRICE_ID=price_standard_monthly
